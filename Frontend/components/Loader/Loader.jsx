import React from "react";
import "./style.css"; // Import the CSS for Loader styling

const Loader = () => {
  return (
    <div className="custom-loader-container">
      <span className="custom-loader-text">Travel Go</span>
    </div>
  );
};

export default Loader;
